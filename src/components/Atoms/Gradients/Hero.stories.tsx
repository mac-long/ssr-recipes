import { StoryObj } from "@storybook/react";

export default {
	title: "Components/Atoms/Gradients/Hero",
	component: Gradient,
};

type Story = StoryObj<typeof Gradient>;

export const Default: Story = {};
